rootProject.name = 'RecipeManager'

include ':Compat'

if ("true".equalsIgnoreCase(System.getenv("RECIPE_MANAGER_MCMMO"))) {
    include ':Compat:mcMMO'
    findProject(':Compat:mcMMO')?.name = 'mcMMO'
}
